interface:
  display_name: "gstack-unfreeze"
  short_description: "Use when you want to remove edit restrictions set by /freeze or /guard and allow editing anywhere again. Triggers..."
  default_prompt: "Use gstack-unfreeze for this task."
policy:
  allow_implicit_invocation: true
