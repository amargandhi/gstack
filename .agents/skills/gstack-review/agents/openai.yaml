interface:
  display_name: "gstack-review"
  short_description: "Use when the user wants code reviewed before merging — checks for safety issues, structural problems, and..."
  default_prompt: "Use gstack-review for this task."
policy:
  allow_implicit_invocation: true
