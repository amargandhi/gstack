interface:
  display_name: "gstack-retro"
  short_description: "Use when the user wants to reflect on recent work — analyzes commit history, work patterns, code quality metrics..."
  default_prompt: "Use gstack-retro for this task."
policy:
  allow_implicit_invocation: true
