interface:
  display_name: "gstack-freeze"
  short_description: "Use when you want to restrict edits to a specific directory — prevents accidental changes outside a scope boundary...."
  default_prompt: "Use gstack-freeze for this task."
policy:
  allow_implicit_invocation: true
