interface:
  display_name: "gstack-qa-only"
  short_description: "Use when the user wants a QA report without any code changes — report-only bug testing. Triggers on: 'qa report',..."
  default_prompt: "Use gstack-qa-only for this task."
policy:
  allow_implicit_invocation: true
