interface:
  display_name: "gstack-qa"
  short_description: "Use when the user wants to test their app for bugs and fix them iteratively. Supports diff-aware, full, quick, and..."
  default_prompt: "Use gstack-qa for this task."
policy:
  allow_implicit_invocation: true
