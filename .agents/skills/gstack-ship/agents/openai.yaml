interface:
  display_name: "gstack-ship"
  short_description: "Use when the user is ready to ship — syncs with base branch, runs tests, reviews diff, bumps version, updates..."
  default_prompt: "Use gstack-ship for this task."
policy:
  allow_implicit_invocation: true
