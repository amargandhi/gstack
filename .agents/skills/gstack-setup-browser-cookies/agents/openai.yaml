interface:
  display_name: "gstack-setup-browser-cookies"
  short_description: "Use when the user needs to import browser cookies for authenticated testing — imports from Chrome, Arc, Brave, Edge,..."
  default_prompt: "Use gstack-setup-browser-cookies for this task."
policy:
  allow_implicit_invocation: true
