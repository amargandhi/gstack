interface:
  display_name: "gstack-upgrade"
  short_description: "Use when upgrading gstack to the latest version. Handles version detection, backup, and migration. Triggers on:..."
  default_prompt: "Use gstack-upgrade for this task."
policy:
  allow_implicit_invocation: true
